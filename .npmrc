# the subject `npm version` commits under; npm's default is a bare
# "0.4.0", which commitlint lets through (it ignores semver-only
# messages) but which does not match the conventional-commit convention
# the rest of the history follows
message = "chore: release %s"
